package io.github.sadeghi.online_shop.database.model

/*
@Entity(tableName = "massages")
data class Entity(
    @PrimaryKey(autoGenerate = true) val id: Int = 0,
    val massage: String,
    val isFromUser: Boolean
)
*/
